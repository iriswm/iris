version: "3"
services:
  webapp:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    links:
      - "db"
    volumes:
      - .:/app
  db:
    image: docker.io/library/postgres:15.3-alpine
    env_file: .env
